// TODO: const your classes.
// TODO: consider moving this to a constant folder along side of others.
// TODO: consider this moving to .env file and environment class.
class AppConfig {
  static const devMode = true;
  static const isLoggedIn = false;
  static const logHttp = true;
}
